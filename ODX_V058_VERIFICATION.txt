Old.dex Alpha Build 0.58 — verification report
Prepared: 2026-08-20

Scope
-----
This direct-source package is intended to be uploaded over the existing Old.dex repository root.
It contains the v0.57 patch baseline plus the v0.58 source changes needed for this update.

Automated regression checks
---------------------------
Command:
  node --test tests/v057-regression.test.mjs tests/v058-regression.test.mjs

Result:
  17 tests passed
  0 failed

The v0.58 regression checks cover:
- site-wide typography restored to the original scale while the OLD.DEX / ALPHA BUILD / Changelog header remains enlarged;
- custom-background text backing and dark Settings contrast;
- readable, non-compressed Select Units rows;
- permanently expanded/larger Battle Composition option details;
- Magical Lore capitalization, Winds of Magic phase labeling, and lore-specific keywords;
- JSON import entry points for Army Lists, the active builder, and Start New Match;
- native Old.dex and Old World Builder JSON import handling;
- deterministic nested-option IDs and bomb/grenade weapon recognition;
- source-specific weapon rules merged with shared weapon-reference rules;
- centered red Create List Cancel action.

Syntax check
------------
All TypeScript files and every <script setup lang="ts"> block in the included Vue files were transpile-parsed with TypeScript 5.8.3.
Result: 25 source files checked, 0 syntax errors.

Packaging check
---------------
The final archive is tested with zip -T before delivery.

Notes
-----
The DIRECT-SOURCE archive is intentionally not a standalone repository checkout, so repository-wide npm run check cannot be executed from the patch directory by itself. The changed-source regression suite and TypeScript syntax validation above are the package-local verification gates.
